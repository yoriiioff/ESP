# ESP для видео

Этот скрипт обрабатывает видео и добавляет ESP-подобные элементы (как в играх), обводя красными квадратами и подписывая ВСЕ обнаруженные объекты из YOLOv8 COCO dataset (80 классов).

## Возможности

- **Полная поддержка всех 80 классов YOLOv8 COCO dataset:**

  **👥 Люди и животные:**
  - person, bird, cat, dog, horse, sheep, cow, elephant, bear, zebra, giraffe

  **🚗 Транспорт:**
  - car, truck, bus, motorcycle, bicycle, airplane, train, boat

  **🏙️ Уличная инфраструктура:**
  - traffic light, fire hydrant, stop sign, parking meter, bench

  **💻 Электроника и бытовая техника:**
  - tv, laptop, mouse, remote, keyboard, cell phone, microwave, oven, toaster, sink, refrigerator

  **🪑 Мебель:**
  - chair, couch, dining table, bed, toilet

  **🍎 Еда и напитки:**
  - banana, apple, sandwich, orange, broccoli, carrot, hot dog, pizza, donut, cake, bottle, wine glass, cup, fork, knife, spoon, bowl

  **⚽ Спорт и аксессуары:**
  - backpack, umbrella, handbag, tie, suitcase, frisbee, skis, snowboard, sports ball, kite, baseball bat, baseball glove, skateboard, surfboard, tennis racket

  **🌱 Растения и предметы:**
  - potted plant, book, clock, vase, scissors, teddy bear, hair drier, toothbrush
- Красные прямоугольники вокруг объектов
- Подписи на английском языке
- Сохранение оригинального аудио в выходном видео
- Вывод в формате MP4

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```
(не требуется при использовании .exe)

## Использование

### 🚀 Вариант 1: EXE файл (рекомендуется)
Используйте готовый автономный .exe файл скачанный вами:
```bash
esp by yoriiioff.exe ваше_видео.mp4
```
или просто двойным кликом для запуска GUI.

**Преимущества:**
- ✅ Не требует установки Python
- ✅ Все зависимости включены (FFmpeg, PyTorch, etc.)
- ✅ Сохраняет оригинальный звук
- ✅ Размер: ~428MB

### 💻 Вариант 2: Python скрипт
```bash
# GUI режим
python esp_detector.py

# Консольный режим
python esp_detector.py путь_к_вашему_видео.mp4
```

Обработанное видео будет сохранено как `out.mp4` в той же папке, что и исходное видео.

### Примеры:
```bash
python esp_detector.py video.mp4
python esp_detector.py "C:\Videos\my_video.avi"
python esp_detector.py ../videos/input.mov
```

## Поддерживаемые форматы видео

- MP4, AVI, MOV и другие форматы, поддерживаемые OpenCV

## Требования

### Для EXE версии:
- Windows 7+ (64-bit)
- Ничего дополнительно устанавливать не нужно - все включено в exe!

### Для Python версии:
- Python 3.7+
- CUDA-compatible GPU (рекомендуется для ускорения)
- Установленные зависимости из requirements.txt
- Для улучшенного GUI: `customtkinter` (опционально, fallback на стандартный tkinter)

## Примечание

Скрипт использует модель YOLOv8 nano для обнаружения объектов из датасета COCO. Для лучшей производительности можно использовать более крупные модели (yolov8s.pt, yolov8m.pt и т.д.), но они требуют больше ресурсов.

**Особенности модели:**
- Обнаруживаются **ВСЕ 80 классов объектов** из стандартного датасета YOLOv8 COCO
- Стандартный порог уверенности: 0.5 для всех объектов
- Модель обучена на разнообразных повседневных объектах
- **Ограничения:** не обнаруживаются здания, двери, лестницы, земля, трава, естественные деревья (эти объекты отсутствуют в COCO dataset)
- Для специализированных задач (архитектура, карты) нужна другая модель или датасет

**Что обнаруживается:**
- Люди, машины всех типов, светофоры, мониторы, мебель, бытовая техника, растения в горшках
